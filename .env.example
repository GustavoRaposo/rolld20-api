# Anthropic — required (GM narrative via Claude)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI — required only when world lore is configured (embeddings)
OPENAI_API_KEY=sk-...

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=

# Auth — generate with: rails secret
DEVISE_JWT_SECRET_KEY=

# CORS — URL do frontend permitido
FRONTEND_URL=http://localhost:3000

# Rails
RAILS_MAX_THREADS=5
